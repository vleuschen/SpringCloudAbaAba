server:
  port: 80
spring:
  application:
    name: consul-consumer-order
  #consul注册中心地址配置
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        hostname: 127.0.0.1
